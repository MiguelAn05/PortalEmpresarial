[pytest]
testpaths = tests
python_files = test_*.py
# -p no:warnings: las dependencias (passlib, pydantic) escupen avisos de
# deprecación que no son nuestros y tapan el resultado real.
addopts = -q --tb=short -p no:warnings
